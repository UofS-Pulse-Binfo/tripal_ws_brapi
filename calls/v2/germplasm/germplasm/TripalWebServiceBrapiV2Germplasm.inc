<?php
/**
 * Call: Calls, class definition.
 * Fun Fact: The plural of germplasm is germplasm (no "s").
 */
class TripalWebServiceBrapiV2Germplasm extends TripalWebServiceDatabaseCall {
  // Call existing and identical call already setup.
  // Declare existing call.
  //
  // module name / char v + major version number / call name.
  public static $is_alias_of = 'tripal_ws_brapi/v1/germplasm';
}
